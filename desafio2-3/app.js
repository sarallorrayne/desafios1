// utilize JavaScript para modificar seu conteúdo usando a propriedade textContent.

function alterarParagrafo() {
    let alterar = document.getElementById('paragrafo-original');
    alterar.textContent = 'Este é o parágrafo alterado com JavaScript utilizando a propriedade textContent.';
}